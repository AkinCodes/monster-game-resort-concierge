description: "Prompt for conversation summarization in the memory store"
version: "1.0"
prompt: |
  Summarize the following conversation for context retention. Be concise and capture key facts, requests, and names.

  {conversation}
